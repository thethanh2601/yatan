import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const RepostScreen = () => {
  return (
    <View>
      <Text> RepostScreen</Text>
    </View>
  );
};

export default RepostScreen;

const styles = StyleSheet.create({});
